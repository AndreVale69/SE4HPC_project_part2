name: Automating the test
on: [push]
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: try
        run: |
          mkdir build
          cmake -S . -B build
          cmake --build build
          cd build && ctest

